import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        base: {
            main: '#e91e63'
        },
        sub: {
            main: '#1976D2'
        },
        back: {
            main: '#000b1c',
            second: '#001a42'
        }
    }
})

export default theme;